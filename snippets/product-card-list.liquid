<div class="product-card product-card--list">
  <a class="full-width-link" href="{{ product.url | within: collection }}">
    <span class="visually-hidden">{{ product.title }}</span>
  </a>

  <div class="list-view-item__link">
    <div class="list-view-item__image-column">
      <div class="list-view-item__image-wrapper product-card__image-wrapper">
        <img class="list-view-item__image" src="{{ product.featured_image.src | img_url: '95x95', scale: 2 }}" alt="">
      </div>
    </div>

    <div class="list-view-item__title-column">
      <div class="list-view-item__title" aria-hidden="true">
        <span class="product-card__title">{{ product.title }}</span>
      </div>
      {% if bold_compare_at_price > bold_price %}
        <div class="list-view-item__on-sale">{{ 'products.product.on_sale' | t }}</div>
      {% endif %}
      {% if section.settings.show_vendor %}
        <div class="list-view-item__vendor medium-up--hide">{{ product.vendor }}</div>
      {% endif %}
      {% unless product.available %}
        <div class="list-view-item__sold-out">{{ 'products.product.sold_out' | t }}</div>
      {% endunless %}
    </div>

    <div class="list-view-item__price-column">
  <br>
  {% capture product_card_price %}
  {%- render 'applicable-selling-price', variant: product.selected_or_first_available_variant, money_price: product.selected_or_first_available_variant.price -%}
  {% endcapture %}

  {% capture product_card_compare_price %}
  {%- render 'applicable-compare-at-price', variant: product.selected_or_first_available_variant -%}
  {% endcapture %}


  <s>{{ product_card_compare_price | money | strip_newlines }} </s> {{-  product_card_price | money | strip_newlines  -}}  

    </div>
  </div>
</div>
